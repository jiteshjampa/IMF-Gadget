{
  "version": 2,
  "builds": [
    {
      "src": "server.js",  // If your entry file is server.js, change this accordingly
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server.js"  // This points to the file where your app starts
    }
  ]
}
